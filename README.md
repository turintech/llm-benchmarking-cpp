# llm-benchmarking-cpp

## Usage

Build:

```shell
make && make compile
```

Run Main:

```shell
./build/main
```

Run Unit Tests:

```shell
./build/tests/tests [test]
```

Run Benchmarking:

```shell
./build/tests/tests [benchmark]
```
